import styled from 'styled-components'

export const DemoToolsHelpWrapper = styled.div`

`

export const ChinaMapHelpWrapper = styled.div`

    .chinaMap-rule-intro{
        color: var(--color-text);
        line-height: 1.7;
        margin-bottom: .625rem;
        padding-bottom: .625rem;
        text-indent: 1em;
        border-bottom: 0.0625rem solid #eee;
    }

    .chinaMap-rule-modal{
        color: var(--color-text);
        line-height: 1.7;
        margin-bottom: 1rem;

        i{
            font-size: 14px;
            color: var(--theme);
        }
    }
        
`